# Feature Menu

- Edit an order (PATCH + rewrite CSV)
- Delete an order (DELETE + rewrite CSV)
- Filter by status
- Search by customer or item
- Sorting by created date or qty
- Add pagination
- Add new CSV columns (price, address, notes)
- Add quick status update buttons
- Add dashboard stats (total orders by status)
- Add CSV import tool
- Add CSV export button
- Add optimistic UI updates
- Add basic input validation messages
- Add bulk actions (mark many as shipped)
- Add simple audit log CSV for changes
